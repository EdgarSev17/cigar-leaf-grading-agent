# Version de Python con la que se midio todo: 3.12.10
#
# El nucleo -- entrenar, clasificar desde rasgos medidos y sacar las cifras --
# solo necesita estos cuatro:
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
joblib==1.5.3

# Figuras del articulo:
matplotlib==3.11.1

# Solo para MEDIR rasgos desde una fotografia. Como las fotografias no se
# publican, no hacen falta para reproducir las cifras:
opencv-python-headless==5.0.0.93
pillow==12.3.0
pillow-heif==1.5.0
scikit-image==0.26.0

# La celda simulada (opcional): el API de RoboDK se instala por pip.
robodk==6.0.1

# La linea base convolucional, que es la unica pieza que pide tarjeta grafica.
# Se instala aparte, con la rueda que corresponda a tu CUDA:
#   torch==2.11.0  torchvision==0.26.0
