# Python 3.12.10 is the version everything was measured with.
#
# The core -- training, grading from measured features, and the reported
# figures -- needs only these four:
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
joblib==1.5.3

# The article's figures:
matplotlib==3.11.1

# Only needed to MEASURE features from a photograph. The reported figures come
# from the published feature tables, so these are not needed to reproduce them:
opencv-python-headless==5.0.0.93
pillow==12.3.0
pillow-heif==1.5.0
scikit-image==0.26.0

# The simulated cell (optional): RoboDK's Python API installs from pip.
robodk==6.0.1

# The convolutional baseline is the only part that needs a graphics card.
# Install it separately, with the wheel that matches your CUDA:
#   torch==2.11.0  torchvision==0.26.0
